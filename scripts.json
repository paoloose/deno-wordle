{
    "scripts": {
        "build": "deno bundle index.ts ./deno-wordle.js",
        "start": "deno run --allow-net --import-map ./import_map.json index.ts",
        "lint": "deno lint",
        "fmt": "deno fmt",
        "dev": "deno run --watch --allow-net --import-map ./import_map.json index.ts"
    }
}
